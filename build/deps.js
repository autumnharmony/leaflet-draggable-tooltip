var deps = {
	TooltipDrag: {
		src: ['layer/Tooltip.js','layer/Tooltip.Drag.js', ],
		desc: 'Makes tooltips draggable (by mouse or touch).'
	},
};

if (typeof exports !== 'undefined') {
	exports.deps = deps;
}
